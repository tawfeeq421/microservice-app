apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  labels:
    app: mongo-service
    auther: Tawfeeq
spec:
  selector:
    app: mongo-app
  ports:
   - protocol: TCP
     port: 27017
  type: ClusterIP
