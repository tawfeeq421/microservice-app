apiVersion: v1
kind: Service
metadata:
  name: client-service
  labels:
    app: client-service
    auther: Tawfeeq
spec:
  selector:
    app: client
  ports:
   - protocol: TCP
     port: 4200
     targetPort: 4200
  type: LoadBalancer
