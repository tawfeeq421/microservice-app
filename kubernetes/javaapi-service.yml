apiVersion: v1
kind: Service
metadata:
  name: javaapi-service
  labels:
    app: javaapi-service
    auther: Tawfeeq
spec:
  selector:
    app: javaapi
  ports:
   - protocol: TCP
     port: 9000
     targetPort: 9000
  type: ClusterIP
